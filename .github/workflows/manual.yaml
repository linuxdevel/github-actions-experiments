name: Manual application installation

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'What environment do you want to install to?'
        required: true
        default: 'Devlab'
        type: choice
        options:
          - Production
          - Preproduction
          - Staging
          - Devlab

jobs:
  hello_world_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Print Environment
        run: echo "Selected Environment ${{ github.event.inputs.environment }}"
